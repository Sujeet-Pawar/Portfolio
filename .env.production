# Production environment — committed on purpose so Netlify's build uses it.
# IMPORTANT: replace the URL below with your deployed Render backend URL,
# e.g. VITE_API_URL=https://portfolio-backend.onrender.com
VITE_API_URL=https://backend-ewq9.onrender.com
